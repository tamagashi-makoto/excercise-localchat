[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "localchat"
version = "0.1.0"
description = "A local chat application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
localchat = "localchat.cli:main"
